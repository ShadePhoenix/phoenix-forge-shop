import style from "./Home.module.scss";
import FeaturedProducts from "../../container/FeaturedProducts";

const Home = () => {
    return (
        <main>
            <FeaturedProducts />
        </main>
    );
};

export default Home;
